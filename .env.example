# URL base del backend API. Solo disponible en servidor (Server Actions).
API_URL=http://localhost:3001

# Clave secreta para autenticación server-to-server. Solo accesible en servidor.
API_KEY=your_api_key_here
